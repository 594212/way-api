package com.example.way.api

import com.example.way.api.data.repositories.MangaRepository
//import com.example.way.api.data.repositories.PostView
import org.junit.Test
import org.junit.jupiter.api.Assertions.assertLinesMatch
import java.util.stream.Collectors


class MangaTest(val mangaRepository: MangaRepository) {
//    @Test
//    fun shouldFindInterfaceProjection() {
//        val list: List<PostView> = mangaRepository.findMangaByName(name = "Владыка Лука и Клинка")
//        val expected: List<*> = java.util.List.of("Владыка Лука и Клинка")
//        val actual: List<*> = list.stream().map<Any> { el: PostView -> el.name }.collect(Collectors.toList())
//    }

    //"Владыка Лука и Клинка"
}